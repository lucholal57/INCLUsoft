import { AsistenciaPersonal } from './asistencia-personal';

describe('AsistenciaPersonal', () => {
  it('should create an instance', () => {
    expect(new AsistenciaPersonal()).toBeTruthy();
  });
});
